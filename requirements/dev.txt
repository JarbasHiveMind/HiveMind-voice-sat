# Test/e2e deps (the `dev` optional-dependencies extra, see pyproject.toml).
# Pinned to the bus-client 1.x world: the satellite's ovos-audio /
# ovos-dinkum-listener runtime deps require ovos-bus-client<2.0.0, so core /
# agent-plugin stay on their last 1.x-compatible alphas (the 2.x successors
# require ovos-bus-client>=2.0.0a3 and would conflict). hivescope 0.5.0a2
# resolves on 1.x via hivemind-bus-client 0.9.1a1. Bump once ovos-audio /
# ovos-dinkum-listener support ovos-bus-client 2.x.
pytest
hivescope==0.5.0a2
hivemind-core==4.3.0a2
hivemind-ovos-agent-plugin==0.2.0a1
hivemind-plugin-manager==0.6.0a1
