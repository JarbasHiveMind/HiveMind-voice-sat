name: Lint

on:
  pull_request:
    branches: [dev, master]
  workflow_dispatch:

jobs:
  lint:
    uses: OpenVoiceOS/gh-automations/.github/workflows/lint.yml@dev
    with:
      ruff: true
      pre_commit: false
